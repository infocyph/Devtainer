name: Devtainer
networks:
  frontend:
    name: Frontend
    driver: bridge
  backend:
    name: Backend
    driver: bridge

include:
  - docker/compose/http.yaml
  - docker/compose/php-fpm.yaml
  - docker/compose/php-cli.yaml
