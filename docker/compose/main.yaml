name: Devtainer
networks:
  frontend:
    name: Frontend
    driver: bridge
  backend:
    name: Backend
    driver: bridge

include:
  - docker/compose/http.yml
  - docker/compose/php-fpm.yml
  - docker/compose/php-cli.yml
